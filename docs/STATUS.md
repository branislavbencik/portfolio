# Portfolio Build Status

**Last update:** 2026-05-13 (session 58) · **Deploy:** branislavbencik.com

## Pages
`/` → Done (landing) · `/skoala` → Done · `/teatime` → Done · `/schneider` → Done · `/nnspect` → Done · `/sakurabook` → Done · `/crowdberry` → Done · `/reprio` → Landing card only (no detail page; links to live product) · `/playground` → Private design lab (not nav-linked; includes `/playground/lightbox-bg` for per-image lightbox cushion audits) · `/about` → Stub · `/resume` → Not started

## What's Next
**OG verification post-deploy** (session 50 follow-up): once the Vercel preview builds, paste `/`, `/skoala`, `/teatime` URLs into a private Slack DM + LinkedIn draft and confirm the unfurl renders the locked V1 wordmark; run opengraph.xyz / metatags.io against the same routes to confirm `og:image:width=1200`, `og:image:height=630`, `twitter:card=summary_large_image` on root, and that project pages serve the same OG image (Next's metadata-replacement quirk handled via explicit `/opengraph-image` reference in `[slug]`'s `generateMetadata`). **Sync `--border-light-hover` to Figma** (session 51 follow-up): `--border-light-hover` (Zinc 400 / `#A1A1AA`) ships as the DeliverablesBar hover stroke but isn't yet a named Figma variable — add as "Border Light Hover" alongside existing "Border Light" / "Border Strong" so design and code stay synced before any future component adopts it. **Project YAML descriptions are dated** for some projects (e.g. NNspect: "ML platform concept · Full pipeline · 3 user roles" reads 2022; Sakurabook similar) — refresh as a separate content task; OG metadata follows YAML automatically. Other deferred: finish the `docs/DESIGN.md` typography table sync (Display/H1/H2 rows still cite pre-`design-tokens-v2` values per session 47 note); About page; resume page; real favicon monogram; mobile vertical-rhythm parity; 1px section-divider weight vs card frames decision; Zinc-50 canvas tone follow-up; HoverPreview keyboard equivalent (audit P0 deferred — needs design thought on anchor-without-cursor); "Curiouser" footer easter egg keep-or-remove; revisit `SessionStart` dev-server hook + `prune-worktrees` alias; consider stripping `line-height` from `type-*` utilities to make typography composable without `!important` overrides (third cascade conflict triggers the refactor); consider renaming `CitationLink` → `ChipLink` or `HeroChip` (component name no longer matches what it renders); VC Honey serif tier in OG (deferred — V1 minimal at 40px is sans-only); Twitter `site`/`creator` handles (not configured — add if X account registered).

## Session Log
58. Hero H1 swap "Generalist" → "Technical" Product Designer (2026-05-13) — Single-conversation arc on `headline-change` worktree branch. **Positioning shift:** swapped the hero H1 qualifier after layered analysis (initial table + ChatGPT-thread reanalysis + final user-driven scoring). Initial recommendation was to keep "Generalist" based on a chip-collapse argument (label naming what chips prove); reversed after user shared (a) the actual target company list — 13 companies heavy on devtools (HTTPIE, Keboola, Nebius Console), fintech (Revolut, Kraken, Huspy, ČS Lead, PriceFX, Paylocity), and technical B2B SaaS (Sumsub, Avvoka, Kota, Igluu) — and (b) the explicit filter-out list (brand, marketing UI, low-complexity apps, UI polish, maintenance, design-system stewardship, shovel execution). Decisive reframe sourced from the ChatGPT thread: *"What wrong interpretation do you most need to prevent?"* — not "what word describes me most completely?" Final scored table (Technical 8.0 / Systems-minded 7.3 / Design Engineer 6.5 / Analytical 6.3 / Senior PD 5.7 / Generalist 5.5) made Technical the right move on audience-fit and filter-strength; chips prevent collapse into Design Engineer territory. **Files changed:** `src/components/HeroStatement.tsx:12` (live H1, one-word swap); `src/app/playground/page.tsx` (7 occurrences in typography-variant scaffolds); `src/app/playground/typography/page.tsx` (2 occurrences plus a stale `g`-feature note re-pointed from "Generalist" to "Designer" since "Technical" has no g); `src/app/playground/disabled-states/page.tsx` (1 occurrence); `src/app/globals.css` (2 comment-only references for accuracy — no token changes). Subhead unchanged: chips (systems · automations · prototypes · code) + chipless judgment sentence remain locked. **Memory updates (cross-session):** `project_pi_shape_generalist_positioning.md` rewritten with full history (Senior → Generalist 2026-04-23 → Technical 2026-05-13), capability-vs-public-label distinction preserved (π-shape stays accurate internal model); `project_hero_lede_copy_locked.md` H1 swap note appended (lede prose still locked); `project_audience_avvoka_ftmo.md` expanded from "Avvoka + FTMO" to the 13-company target list with category breakdown; `MEMORY.md` index lines updated for all three. **Dev-server hygiene:** at user's instruction, killed three unrelated dev servers running on `:3000` / `:3010` / `:3020` (for `reprio/onboarding-finetune`, `portfolio/remove-links`, `portfolio/critique` worktrees), spun up a fresh server for this worktree on `:3000`. Process-cwd lookup via `lsof -p <pid>` was the diagnostic that disambiguated which port belonged to which worktree (same pattern as session 48/55). **Docs sync at /ship:** README verified — header phrase "Senior Product Designer" is a parallel framing (GitHub repo meta-description, not the portfolio H1); not drift; flagged separately for user decision. `CLAUDE.md` line 3 carries the same parallel framing; same logic — left untouched. `.impeccable.md` / `docs/DESIGN.md` / `GUIDELINES.md` untouched (no token, component, behavior, voice-yaml, or page-table changes per Step 1.7 classification table). Build passes 16 static pages. **Risk surfaced for future work:** "Technical Product Designer" raises the visual-craft floor on the portfolio itself — *"You can reject fancy polish. You cannot look unfinished."* — worth a precision pass on chrome (typography, spacing, hover states, edge cases) in a separate session before shipping the rest of the portfolio under this positioning. Not blocking this PR.

57. Skoala case-study rebuild + light-theme lightbox (2026-05-11) — Multi-conversation arc on `skoala-case-study-update` worktree branch that grew from "implement the updated Skoala page from Figma" into a system-wide rule lock-in for image framing and lightbox parity. **Skoala restructure**: 2 body sections → 6 — Context ("A growing content library needed a production system"), Core content model ("The teaching module was our primary building block"), two Key design decisions ("Start constrained, expand intentionally" + "Edit live content without breaking it"), Outcome ("From volume to coverage" with awards), Reflection ("Systems as guardrails"). Intro/description rewritten ("Skoala helps Czech teachers bring financial literacy to classrooms…"), contributions rephrased to lede/scope/architect form, multiple image captions reassigned, "I designed" → "I architected" in the lifecycle section, content roadmap caption locked in. **Schema additions**: new `backgroundShade: "1" | "white"` knob on the image block — locked to two tokens (#F4F4F5 surface-1 default + #FFFFFF surface-tile); user explicitly rejected `surface-2` after a brief experiment, surfaced in memory as feedback rule. New section-level `awards` array (logo + name + description) renders below images via new `AwardList.tsx` component. New `LogoRow.tsx` component for adjacent `variant: 'logo'` image runs; renders centered horizontal strip. **Lightbox 1:1 mirror rule**: standalone `lightboxBackground` field removed from schema + 5 YAML entries across nnspect, teatime, sakurabook, skoala stripped. Lightbox now renders the same `framingClass()` output as the body (extracted as a shared helper in `CaptionedImage.tsx`); `LightboxContext` registration carries `background`/`backgroundShade`/`paddingSides`/`borderSides`/`cornerRadius`. `ProjectHeader.tsx` registration updated to pass explicit `paddingSides: "none"` + `borderSides: "all"` so the hero's lightbox view matches its body (it had been silently diverging). **Lightbox theme inversion**: backdrop `bg-text-primary` → `bg-canvas` (#FAFAFA), caption + controls + counter swapped to `text-text-primary` / `text-text-secondary`, focus rings re-themed `ring-text-primary/40 ring-offset-canvas`. Tall-portrait fix: removed double `max-h-[80vh]` cap on wrapper (kept on image only), then bumped image cap to `max-h-[88vh]` to recover visual height on portrait images like the 1920×2217 published-module screenshot. **File-name hygiene pass**: 8 Skoala body images renamed to flat numeric sequence (`skoala-context.png` → `skoala-1.png`, …, `skoala-ws2-i2.png` → `skoala-8.png`); thumb + awards keep semantic names; leftover `skoala-ws1-i1.png` deleted. **Awards data shipped**: EIFLE Award (Won Children's Education Program of the Year) with logo; MAIA Award (Nominated in top 16) with logo. **Multi-round conversational iteration with the user surfaced several long-standing naming/content drifts** (e.g. user's mental model of "slide-taxonomy" was the published-module Předlužení view, not the System/Static/Inputs/Outputs grid) — resolved by overlaying `~/Desktop/skoala-learning-module.png` onto the slide-taxonomy slot and treating it as authoritative. **Memories written**: `feedback_image_backgrounds_two_options_only.md` (lock the surface-1 / surface-tile constraint system-wide), `feedback_lightbox_mirrors_detail_one_to_one.md` (lock the 1:1 framing parity rule). **Docs sync at /ship**: `docs/DESIGN.md` § 7 (Case Study Image Modes) extended with `backgroundShade` knob documentation + lightbox-parity rule; README verified clean (no stack/route/dep changes); CLAUDE.md / .impeccable.md / GUIDELINES.md untouched (no convention/voice/behavior shift beyond what DESIGN.md now captures). Token audit clean — 77 pre-existing violations unchanged. Build passes 16 static pages.

56. NDA-gated locked-card pattern for chips + deliverables (2026-05-08) — Multi-conversation arc on `remove-links` worktree branch that started as "remove design-sprint Figjam links because they're under NDA" and resolved into a positioning move: replace silent removal with an honest **locked-state treatment** signaling "I work with NDA-tier clients AND respect those agreements." Five-plus rounds of visual iteration via `/playground/locked-card-finalists` (with **shared image-tilt handles** stress-testing pill chrome across darker/lighter image conditions) before landing the final spec. **Final shipped pattern**: chip stays styled identically to active chips at scan-mode (locked state lives entirely in the hover card); on hover, card layers a baseline `oklch(0% 0 0 / 0.05)` black scrim across the image plus a corner pill anchored `top: 12px right: 12px` containing "NDA" 10px mono uppercase letter-spacing 0.12em + 12px Lock icon, both at `text-primary`; pill chrome `oklch(99% 0 0 / 0.92)` bg + `1px solid var(--surface-2)` border + `backdrop-filter: blur(2px)` — same surface tokens as the card itself, idiom is **stamp on document** not notification badge; pill uses existing `.locked-card-badge-enter` (220ms) + `.locked-card-lock-snap` (240ms) keyframes with reduced-motion guards; caption stays at `text-primary` with no `↗` arrow (locked chips have no destination). On `(hover: none)` or viewports ≤1023px the chip renders as **plain prose** (no chip styling, no `↗` glyph) since hover card can't fire and there's no destination — closes the mobile UX question without bolting on a tap-to-show-card pattern. **DeliverablesBar parallel treatment**: disabled tile renders with `aria-disabled="true"`, opacity 0.5, no pointer events, label at text-secondary, trailing slot replaces `↗` with inline NDA mono badge + 12px Lock icon (same vocabulary as hover card). **Iteration arc that produced the final**: started with image-opacity dim → tried in-caption badge → top-right corner badge → centered dark scrim 0.4 → 0.3 → 0.15 (F2-current) → review caught "six channels carrying a one-bit signal" + image-coverage cost → playground Round 1 (active reference + F2-current + F2-corner + caption-strip Option B variants) settled corner placement and killed Option B (caption-strip lock substitution failed evidence test — too peripheral at hover-card scale) → Round 2 explored 6 icon-only pill chromes (baseline / inert-dark / inert-light-gray / tonal-surface / outline-only / shadow) → user found only F2-current + F2-corner + corner-icon-only read as locked, others discarded → review caught the deeper miss: **chrome and content can't be solved separately** — the "white pill reads as active notification" framing was correct for unlabeled icons but wrong for labeled stamps; explicit "NDA" word transforms same chrome from notification widget into deliberate gating mark (physical-document stamp idiom) → Round 3 rendered Final A (white-pill stamp) vs Final B (surface-1 gray stamp) with **shared image-tilt handles** (darken + lighten scrim sliders) so user could stress-test contrast across realistic image conditions → Final A picked, plus user requested 0.05 black scrim baked in for safety baseline. **Round-2-to-3 reversal recorded**: my Round 2 framing was wrong about the white pill — surfaced explicitly in plan file context section as a calibration data point for future review work. **Files changed**: `src/components/HoverPreview.tsx` (locked-card branch swapped: scrim from 0.15 zinc → 0.05 black, center wrapper dropped, pill anchored top-right, caption color override removed); `src/components/CitationLink.tsx` (added `disabled` prop with `chip-locked` class, `onClick={preventDefault}`, NDA-aware `aria-label`, optional `lockedPreviewSrc`/`lockedPreviewCaption` data-attrs feeding HoverPreview); `src/components/DeliverablesBar.tsx` (disabled tile branch with inline NDA + Lock badge); `src/components/HeroStatement.tsx` (`prototypes` chip flipped to `disabled` with Schneider preview wired); `src/components/icons/Lock.tsx` (new 20×20 outline icon, currentColor stroke for parent-color inheritance); `keystatic.config.tsx` (added `disabled: fields.checkbox()` to deliverables array schema); `src/app/[slug]/page.tsx` (passes `disabled` flag through both DeliverablesBar invocations); `src/app/globals.css` (locked-card keyframes + reduced-motion guards + chip-locked mobile prose render via `@media (hover: none), (max-width: 1023px)` with raised specificity `a.chip-link.chip-locked` to outrank browser default `a:link`); `src/content/projects/schneider.yaml` + `src/content/projects/skoala.yaml` (restored deliverables marked `disabled: true`). **Mobile media-query gotcha caught and fixed**: `@media (hover: none)` doesn't fire in desktop devtools mobile-simulator (mouse stays input device); fixed with OR combination `@media (hover: none), (max-width: 1023px)` so simulator triggers via viewport width. **Cleanup**: deleted three superseded playgrounds (`locked-card-tweaks`, `locked-dark-scrim`, `locked-card-finalists`) post-decision; `disabled-states` exploration playground retained as reference. **Docs sync at /ship**: `docs/DESIGN.md` § Hover Preview Card adds "Locked variant" bullet documenting corner pill + scrim recipe + animation reuse + caption parity; `GUIDELINES.md` § Accessibility adds "NDA-gated artifacts use a locked-card pattern" paragraph documenting the aria-label, mobile prose render, and DeliverablesBar parallel treatment with rationale (silent removal erases work; locked-card pattern is honest *and* respects the agreement). README verified clean (no stack/route/dep changes — all playgrounds created and deleted within session). `CLAUDE.md` / `.impeccable.md` untouched (no convention or voice shift; component-level pattern lives in DESIGN/GUIDELINES). Build clean, 13 static pages.

55. Schneider Electric selected project + per-image lightbox cushion (2026-05-05) — Multi-conversation arc on `schneider-project` worktree branch via `/shape` discovery + `/ship`. **New `/schneider` page** "Configurator for electrical panel builders" (Schneider Electric, 2024) shipped as the first selected project in chronological order — slots ahead of NNspect via order field bumps (NNspect 3→4, Sakurabook 4→5, Crowdberry 5→6). 5-day design sprint prototype for guided panel-builder configurator (PV / EV / BESS) generating priced bill of materials. **Locked copy** through scored 0–10 tables: title "Configurator for electrical panel builders" (echoes description's "panel builders" wording vs. weaker "electrical panel design"); 3 chips significance-led `Design Sprint · Energy Infrastructure · Prototype` (3-tag mix breaks 2-tag peer convention but earned by domain+method+state coverage; "Prototype" without "Figma" prefix avoids word-duplication with bottom deliverable link); description S3 "Prototyped in a 5-day sprint." (verb echoes the third tag, "sprint" disambiguated by chip rail). **Bottom DeliverablesBar slot for selected projects** — case studies have it at top (between hero and body, current behavior); selected projects now render it after body content via new `!isCaseStudy && project.deliverables.length > 0` conditional in `[slug]/page.tsx`. Two links shipped: Figma Prototype (same URL as LP hero CitationLink for link unity) + Figjam Design Sprint board (process artifact, makes the 5-day claim provable). **CaptionedImage `cornerRadius` prop** — opt-in `"sm" | "md"` that applies a corner radius even when there's no border. Scoped to `resolvedBorder === "none"` to avoid radius collision with the existing border-bound radius. Used on schneider-ws1 (questionnaire grid screenshot with baked-in light-gray bg — wants rounded corners but no wrapper border). **Lightbox refactor — global cushion stripped, per-image opt-in** — original Lightbox always wrapped images in `p-6 + bg-surface-1` (or `bg-canvas` when `background: false`); refactored so default is bare image on dark backdrop, with optional cushion via new `lightboxBackground` field on image schema (`"" | "surface-1" | "canvas"`). 6 of 27 images ship with explicit cushion: skoala-ws2-i1 + teatime-ws2-i1 use `canvas` (#FAFAFA / Zinc 50, for diagrammatic content with `width: 640`); teatime-ws2-i2 + teatime-ws3-i1 + nnspect-ws2 + sakurabook-ws2 use `surface-1` (subtle off-white, for product screenshots that benefit from a soft frame). Border decision: zero images use border in lightbox — global no-border default holds. **Audit playground** at `/playground/lightbox-bg` — lists all 27 unique images deduped (cover-as-first-section-image dedup handled for selected projects), per-image bg + border toggles, dark backdrop matching lightbox modal, drift marker (←) flags any image where toggle differs from YAML default, copyable output block. Built as decision instrument for the cushion audit; left in place for future re-audits. **Image 3 BOM swap** mid-session — user caught a mistake in the original `schneider-ws2.png` export and dropped a corrected version on Desktop; bit-identical replacement via `cp`. Browser hard-refresh required because `next.config.ts` runs `next/image` `unoptimized: true` (no fingerprinted URLs). **Worktree-port footgun caught** at first verification: `lsof` confirmed `:3000` was running for `critique` worktree, not `schneider-project` — started new dev server on `:3001` (Next auto-bumped past taken port). Same pattern as session 48; the `lsof -p <pid> -d cwd` resolution remains the canonical diagnostic. **Docs sync at /ship**: `README.md` "Selected projects" line gets `/schneider` prepended (chronological: 2024 → 2022 → 2021 → 2020); `CLAUDE.md` Pages table row updated (`/nnspect, /sakurabook, /crowdberry` → `/schneider, /nnspect, /sakurabook, /crowdberry`). `docs/DESIGN.md` / `GUIDELINES.md` / `.impeccable.md` untouched per Step A classification — DESIGN.md mentions CaptionedImage by name (line 215, widths) and lightbox triggers (line 153, focus-ring-card) but doesn't enumerate every prop; GUIDELINES.md lightbox references (touch targets, button-vs-link, focus-mgmt) all still accurate after the refactor. Token audit: 43 violations unchanged (all pre-existing in `src/app/playground/page.tsx` + `playground/typography/page.tsx`, not introduced by this session). Build passes 15 static pages including `/schneider` as new SSG entry and `/playground/lightbox-bg` as new static playground.

54. Cleanup audit + playground deletions (2026-05-04) — Single-arc cleanup pass on `cleanup-audit` worktree branch (renamed mid-session from `correct-prototype-link` after PR #37 was closed; the trivial Schneider href swap that originally scoped that branch was abandoned per user — they had restored the old URL after re-evaluating). **`/audit` skill scored 16/20** (Excellent regressed from 18/20 at session 42), surfacing two P0 items: (1) `CitationLink.tsx` shipped without keyboard `focus-visible` ring — STATUS.md had flagged it as deferred, audit elevated it to P0 since the chip is the primary affordance on the hero; (2) `HoverPreview.tsx` has no keyboard equivalent (desktop-hover-only) — deferred to STATUS.md "What's Next" since it needs design thought on anchor-without-cursor. **CitationLink fix shipped:** added `.chip-link:focus-visible` rule in `globals.css` using `outline` (not `ring`) to avoid layout shift on inline prose — `outline: 2px solid var(--citation-link); outline-offset: 2px; border-radius: 2px`. **Playground deletions** (per STATUS.md "What's Next" queued list, sessions 49 + 51 follow-ups): deleted `src/app/playground/hero-citations/` (combo C shipped), `src/app/playground/deliverables-bar/` (variant B Zinc 400 shipped), `src/components/playground/HeroCitationsVariant.tsx`, `public/images/hero-previews/skoala.svg` (orphan). **Relocation:** `src/components/playground/HoverPreview.tsx` → `src/components/HoverPreview.tsx` (live consumer is `HeroStatement.tsx`); empty `src/components/playground/` directory removed; relative import `"../icons/ExternalArrow"` → `"./icons/ExternalArrow"` adjusted post-move. Import in `HeroStatement.tsx` updated `"./playground/HoverPreview"` → `"./HoverPreview"`. **CSS cleanup in `src/app/globals.css`** — coupled rule + `@source inline()` directive deletions per Tailwind 4 purge rules: deleted `--text-tertiary` token + `.text-prose-primary/secondary/tertiary` + `.chip-color-primary/secondary/tertiary` + `.chip-underline` + `.thumb-240/320` + `.card-style-light/museum` + `.shadow-off`. Kept `chip-link`, `chip-arrow`, `chip-color-blue`, `thumb-280`, `card-style-casestudy`, `shadow-on` (live consumers in `HeroStatement` + `CitationLink` + `HoverPreview`). Comment blocks rewritten to drop references to deleted variants (museum / playground / shadow-toggle framing). **Retention decisions:** `/playground/page.tsx` (hero variants reference) and `/playground/typography/page.tsx` and `/playground/og-preview/` all kept — sandbox code, no production routing, audit P2 violations there are pre-existing per `feedback_visual_variants_via_playground` retention memory. **Branch surgery:** PR #37 closed mid-session, branch renamed `correct-prototype-link` → `cleanup-audit`, hard-reset to `origin/main`, old remote ref deleted. Worktree directory keeps old name (cosmetic, cleanup deferred to merge-time). **Verification:** build clean (15 pages → 13 pages, hero-citations + deliverables-bar gone); token-audit unchanged at 43 violations (all pre-existing in retained playground sandboxes — `feedback_visual_variants_via_playground` memory exempts them); grep guard for deleted class names returns clean (no orphan references in `src/` or `public/` outside `globals.css` itself). **Docs sync at /ship:** `docs/DESIGN.md:131` (Hover Preview Card Sizing line) stripped references to deleted `.thumb-240/320` and corrected the image dimensions from "320×180" to "280×158" (the actual live size); `docs/STATUS.md` "What's Next" stripped the entire "Playground cleanup PR queued" sentence (now done) and replaced "citation-link keyboard focus state" deferred item with "HoverPreview keyboard equivalent" (the remaining P0). README verified clean (no stack/script/route changes — README only references `/playground` generically). CLAUDE.md / GUIDELINES.md / .impeccable.md untouched (the focus-visible addition is covered by GUIDELINES § Focus is always visible, no new rule needed). **Audit P1 deferred to follow-up:** `--border-light-hover` is hardcoded as a `:root` CSS variable but not exposed via `@theme` and not yet a Figma variable — already tracked in STATUS.md as session 51 follow-up, no action this PR. **`/impeccable extract` pass** (run after audit fixes): single clean candidate emerged from a 6-pattern survey — the card focus-ring combo (`focus-visible:ring-2 focus-visible:ring-text-primary focus-visible:ring-offset-2 focus-visible:ring-offset-canvas`) was repeated identically across 5 component files. Extracted as `.focus-ring-card` in `globals.css` (`box-shadow: 0 0 0 2px var(--canvas), 0 0 0 4px var(--text-primary); outline: none`) — uses double-shadow inset to recreate Tailwind's ring-offset behavior; element's own `border-radius` governs the curve. Migrated 5 call sites: `CaseStudyCard.tsx:27`, `PlaygroundCard.tsx:26`, `CaptionedImage.tsx:52`, `ProjectHeader.tsx:80`, `Footer.tsx:143`. Survey originally counted 4 sites — Footer email button caught only at build-time grep guard, escalating the extraction's value (5 instances vs 4 strengthens the threshold case). `DeliverablesBar.tsx:31` left intentionally on its `focus-visible:ring-inset` variant — the tile has its own border that the focus ring must sit *inside* (documented as an exception in the new DESIGN.md § Focus States). Other 5 candidates from the survey (card hover scale, arrow translate, lightbox controls, zoom-trigger pattern, kicker+headline stack) were below threshold or anti-extractions (visually similar / semantically distinct). **Documentation** added: new "### Focus States (canonical primitives)" subsection in `docs/DESIGN.md` § Component Architecture documenting both focus primitives — `.chip-link:focus-visible` for inline prose (outline-based, no layout shift on glyph baselines) and `.focus-ring-card` for block-level surfaces (box-shadow, follows border-radius, works inside overflow:hidden) — with explicit rationale for why two primitives exist instead of one (outlines clip on rounded corners + overflow; box-shadow rings have layout-shift-free spacers that are pointless on inline glyphs). Build clean, 13 static pages.

53. Detail hero metadata pattern + tag content cleanup (2026-05-04) — Single-conversation arc on `fix-detail-metadata` worktree branch that decomposed the case-study hero kicker and trimmed weak tags. **Two-slot hero pattern:** replaced the dot-separated allcaps run that conflated identity (company, year) with facets (impact metric, role, method) into one crowded line above H1. New shape: kicker `COMPANY · YEAR` only above H1 (identity, `type-allcaps text-text-secondary`), then H1, then intro, then a chip rail below intro using the same `type-tag-chip` class as the LP `CaseStudyCard` chips — byte-for-byte parity creates a "ticket-stub" visual handshake from LP click → detail arrival. **Comparison-table-driven decision** (per `feedback_quantified_comparison_tables` memory + status-quo-column rule): scored 4 position options (status quo, above-H1-split, between-H1-and-intro, below-intro) on read-flow, info-hierarchy, magazine convention, direct-arrival visitors, LP→detail continuity, crowding; below-intro won. Separate scoring of 4 design options (status quo, bordered chips 1:1, subtle inline, pill chips); 1:1 with LP won. Decision rationale: meta on detail = secondary info (already seen on LP) while intro = new info (first contact), so meta belongs after intro by recency-of-novelty; chip rail also acts as "you arrived at the right place" receipt that only works after the reader confirms expectation via the intro. **Refinements locked:** tag order is significance-led (leftmost = strongest signal for that project), with quantified outcomes > role > method as a tiebreaker but not a hard rule (Co-owner can earn first slot when ownership is the headline). Position-as-tag retained — not all projects have an interesting role to surface, forcing a "Role:" meta row would create N/A states. **Implementation:** `ProjectHeader.tsx` gains `company` prop, kicker rendered inline (filtering empty values), chip rail rendered conditionally with `type-tag-chip` flex-wrap below intro at `mt-3` (matches existing intro→H1 treatment). `ProjectMetaRow.tsx` retired — single-consumer abstraction stopped earning its keep at 2 fixed values. `[slug]/page.tsx` wires `company={project.company}` through. **Tag content cleanup** (separate quality pass after design lock): removed `Concept` from `nnspect.yaml` (transparency about pre-ship status is body-copy work, not chip-rail work — the chip demoted the project before it got read), removed `Ecosystem of multiple apps` from `sakurabook.yaml` (long, and case-study evidence shows one template + one plugin — the chip wrote a check the body didn't cash), removed `Usability testing` from `crowdberry.yaml` (generic methodology label, didn't communicate the regulated-investment-flow stakes that made it interesting). Editorial principle articulated: tags should *promote or add information*, not fill a 3-slot grid. NNspect, Sakurabook, Crowdberry now ship 2 chips; Skoala and TeaTime keep 3. **Verification:** SSR HTML curls confirmed all 5 detail pages render the new kicker + chip rail in YAML order; Playwright screenshots at 1440 desktop + 390 mobile across all 5 detail pages confirmed visual parity with LP cards. Token audit clean — 43 violations exist but all pre-existing in `src/app/playground/*`, none introduced by this session. Build passes 15 static pages. **Memories written:** `project_detail_hero_metadata_pattern.md` (locks the two-slot hero pattern + 1:1 LP↔detail chip parity rule + ProjectMetaRow.tsx deletion), `feedback_tag_order_significance_led.md` (locks the variable-by-significance ordering rule with the numbers > role > method tiebreaker; no fixed-slot rule, position can land anywhere). **Docs sync at /ship:** README verified clean (no stack/script/route/dep changes); CLAUDE.md / .impeccable.md / docs/DESIGN.md / GUIDELINES.md untouched (no token, component, behavior, voice, or page-table changes per Step 1.7 classification table — modified existing component, no new patterns matching the table's rows).

52. LP card thumbs — explore-and-abort + surgical asset fix (2026-05-03) — Multi-conversation arc on `lp-cards-padding` worktree branch that started as a system-level direction question ("should LP cards adopt a uniform gray padded frame as a system signal?") and resolved to a per-asset content fix after a playground-driven reversal. **Plan-mode iteration through 3.5 versions:** v1 argued "don't ship padded, fix the asset" based on a misread of `project_positioning_shift` memory (treated Vercel/Linear as visual anti-references when the memory only excludes them as voice references and explicitly notes they're valid as interface-density references); v2 proposed selective treatment by deliverable type (padded for projects without full-app surfaces, full-bleed for the rest) — overruled by user's systems-coherence pushback ("six tiles in one consistent treatment reads as 'I designed a system and applied it'; mixed reads as 'I made tiles'"); v3 swung to all-padded uniform with content adapting per project; v3.5 added Reprio-as-tier-marker carve-out (row label disambiguates) plus extended padded frame to detail page hero (with body items staying flat per dual-register principle). **Playground built as decision instrument:** `/playground/lp-cards` rendering all 6 projects at real LP dimensions across padding values (32/48/64/96/off) × backgrounds (white/Zinc 50/Zinc 100), sticky toggle bar pattern lifted from `/playground/hero-citations`, real `PlaygroundCard` component used for Reprio tile so the comparison reflected production register (not a hardcoded `bg-[#0a0a0a]` approximation that violated the no-hardcoded-colors rule). **Reversal at the playground:** user eyeballed and replied "honestly im not convinced all of this is worth it." Three concrete costs articulated: (1) slight loss of minimalism — current cards already at floor of containment (4px outer + thin border), gray inset adds a third containment layer compounded across 6 tiles in column; (2) image shrinks ~13% in width to pay for padding tax; (3) system case was contingent on commissioning ALL 6 covers as deliberate hero-shots — without that, padding adds redundant containment to projects with full-app screenshots (Skoala/NNspect/Crowdberry) where the screenshot already has its own visual structure. The "padding equalizes weak covers" gain only equalizes UPWARD if every cover is re-commissioned; otherwise cost is paid uniformly while gain concentrates in 2 projects. **Surgical fix shipped:** Teatime cover replaced with focused tablet+report PNG on light-blue baked-in canvas (1-to-1 swap, second pass borderless after first pass had asset-side borders that would have double-bordered against the existing card chrome); Sakurabook 3-step rearrangement (delete old `sakurabook-ws1.png` availability-states standalone → rename old `sakurabook-thumb.png` → `sakurabook-ws1.png` so the listing-detail screenshot moves into body position 2 → add new `sakurabook-thumb.png` showing booking sidebar + timepicker cards as the curated artifact); YAML alt+caption swap on `sakurabook.yaml` images[0]/images[1] so descriptions travel with image content (not file path). Added "Polaris Design System" as Sakurabook 2nd tag (between i18n and Ecosystem of multiple apps). **No CSS / token / schema changes** — entire shipped delta is 3 PNG swaps + 1 YAML edit. Asset carries its own framing; code stays ignorant of treatment differences. **Playground cleanup same session:** `/playground/lp-cards` route + `LpCardsPlayground.tsx` component deleted post-decision (no production consumer; served its purpose as decision instrument). **Memory updates:** rewrote `project_positioning_shift.md` to clarify Vercel/Linear/Warp are NOT voice references but ARE valid visual/interaction references (dropped the "don't push Linear/Vercel clone" framing per user's direct correction); added `feedback_visual_coherence_over_conceptual.md` (durable insight: at LP scan moment, visual coherence beats conceptual coherence because viewers pattern-match before they decode rationale — applies broadly to homogeneous component families even though the specific LP-card system that prompted it was abandoned); added `feedback_register_lives_in_body_not_chrome.md` (durable insight: when applying dual-register principles, read-mode flatness lives in body content not page chrome — containment idioms can bridge LP→detail-hero without violating the principle); rewrote `project_lp_card_padded_system.md` from "system to ship" → "explored-and-aborted record with abort reasoning" so future sessions don't re-litigate without context. Plan file at `~/.claude/plans/consider-if-my-positioning-compressed-kitten.md` annotated with Status header noting the system was explored-and-aborted and surgical fix shipped instead. **Asset workflow lesson recorded:** when an asset will sit inside an already-bordered container, export it borderless from Figma — let the parent container own the frame. Caught on second-pass re-swap; first-pass assets had asset-side borders that would have double-bordered against `CaseStudyCard.tsx:30`'s existing border. **Docs sync at /ship:** README verified clean (no stack/script/route/dep changes — playground was created and deleted within session, never reached the route table); CLAUDE.md / .impeccable.md / docs/DESIGN.md / GUIDELINES.md untouched (no token, component, behavior, voice, or page-table changes — entire shipped delta is content-side asset swaps + factual tag addition + caption/alt swap traveling with image content). Build clean (15 static pages including `/sakurabook`, `/teatime` regenerated; `/playground/lp-cards` correctly absent from route list). No token-audit violations introduced.

51. DeliverablesBar component + semantic border tokens (2026-05-02) — Multi-conversation arc on `deliverables-bar` worktree branch shipping a flat tile stack below the Impact Bar on case studies, surfacing concrete artifacts (live products + automation flows + FigJam boards). Skoala renders 2 tiles (`skoala.cz` + Design Sprint Figjam); TeaTime renders 2 (`teatime.cz` + n8n speech-evaluation workflow). **Component shape (after iteration):** 47px tile (`px-6 py-3` + 15px label), separated tiles (`flex flex-col gap-2`) — not a joined toolbar; pulled back from initial `type-body` 18px + `py-6` 24px after user feedback "too big". Optional 12px caption renders below the label when present. Earlier two-line label+caption variant collapsed to single-line label after `/emil-design-eng` review on n8n tile (caption was duplicating the link's natural context). **Hover treatment** went through 4 iterations before landing: started with `inset 1px var(--text-primary)` (Zinc 900, ~90% black) → user "still insane, 90% black does not feel quiet" → tried `--border-strong` (Zinc 300, ~7-point delta) → user concerned about visibility → built `/playground/deliverables-bar` comparing 5 variants (Zinc 300 / Zinc 400 / Zinc 500 / no-border-darken / underline-on-label) at real dimensions → variant B (Zinc 400, ~27-point delta) won → ships as `hover:border-[var(--border-light-hover)]` with 200ms `ease-out` + arrow translate `(2px, -2px)`. Arrow remains the **primary** transform-tier affordance; border darken is supporting hairline. **Token system:** added 3 semantic border CSS variables in `:root` matching Figma source-of-truth — `--border-light` (Zinc 200, "Border Light"), `--border-strong` (Zinc 300, "Border Strong", reserved with no current consumer), `--border-light-hover` (Zinc 400 `#A1A1AA`, hover companion to `--border-light`, not yet in Figma — flagged in What's Next for sync). All three intentionally **not** exposed via `@theme inline` to keep the Tailwind utility namespace clean; consumed via arbitrary-value syntax (`border-[var(--border-light)]`). User explicitly renamed proposed `--border-stronger` → `--border-light-hover` per `feedback_avoid_confusable_token_names` memory ("border-stronger" too easily confused with "border-strong"). **Bugs caught and fixed mid-session:** (1) Stale `.next/` cache served compiled CSS without new `--border-*` rules — `var(--border-light)` resolved to unset → `currentcolor` → text-primary black (the rest border briefly rendered ~90% black instead of Zinc 200); root cause is Turbopack's hot-reload not picking up `:root` CSS variable additions, fixed reproducibly by `rm -rf .next/` + dev server restart. (2) Double border on hover — initial implementation had a wrapping `<div>` with rest border + inner `<a>` with `inset` shadow on hover, producing two concentric border lines; collapsed to single `<a>` carrying both rest and hover treatment, switched from `transition-shadow` + `inset shadow` to `transition-colors` + real `border-color` swap. **Schema:** added `deliverables` array field to Keystatic case-study schema (label, href, optional caption); selected projects leave the array empty. Conditional render in `[slug]/page.tsx` — only shows when `project.deliverables.length > 0`. **Docs sync at /ship:** `docs/DESIGN.md` § 2 Borders rewritten with semantic token names alongside existing zinc references; new § Deliverables Bar entry in Component Architecture covering layout / dimensions / hover model / focus / a11y / reduced-motion / why-border-color-not-inset-shadow rationale. `GUIDELINES.md` § Motion gets a carve-out: "border-color shifts allowed only when transform isn't viable, and only as supporting hairline" — names the bind (text-only tiles can't transform per `feedback_no_hover_scale_on_text` memory, but still need hover affordance beyond a single icon translate) and pins guardrails (max one tier on border ramp, transform stays primary). Real conflict between memory rule and shipped reality; better to document the carve-out than leave the rule absolute. README verified clean (no stack/script/route changes — `/playground/deliverables-bar` not nav-linked). Memory writes deferred (no novel rule emerged that isn't already captured). Build passes 15 static pages including the new playground route.

50. Custom OG share-preview system + signature wordmark image (2026-05-02) — Multi-conversation arc on `og-images` worktree branch via `/shape` discovery, that replaced the stopgap avatar-thumbnail share preview with a single signature OG image serving every route. **Discovery via parametrized playground** (saved as `feedback_external_render_surface_chrome` memory: external-render surfaces need platform chrome, not bare canvas): scaffolded `/playground/og-preview` rendering 1200×630 candidates inside Slack/LinkedIn/X/iMessage chrome at native widths (540/552/600/280px) via `transform: scale(target/1200)` with `transform-origin: top left`. Sticky toggle bar drove three axes (project · route · wordmark size) over a single parametrized `V1Wordmark` component, replacing what would have been ~12 static frames. Five variants explored (V0 status quo, V1 wordmark+role+avatar+border+domain, V2 cover-led, V3 combo); after iteration, **V1 reduced to wordmark only at 40px left-aligned vertically-centered on `#FAFAFA` canvas** — no role text, no avatar, no border, no domain text. Rationale: image earns its slot by being a *signature*, not a duplicated label, since role + name already render in chrome metadata on every platform (Emil's "what you don't add" + `/clarify` "don't repeat"). **Quantified path-comparison table** with status-quo column scored 5 implementation paths; original "Phase 1" picked Hybrid (E, 8.6/10) but collapsed to "Single unified" (B) once V1 didn't vary per route — picking dynamic-per-`[slug]` is no longer needed. **Layered share-preview content strategy** (image=signature wordmark · title=route content · description=π-shape capability), shipped via three-axis edit: (1) root `og:title` em-dash separator "Branislav Benčík — Product Designer" (was pipe); (2) project `og:title` drops `— Branislav Benčík` suffix entirely (just `${project.title}`) — name already in OG image, also reduces iMessage truncation pressure; (3) root description rewritten "Portfolio of Branislav Benčík. Product designer fluent in B2B systems and code." → "I design complex systems and ship code." after user rejected "B2B systems" (he's applying to FTMO, B2C). Description copy ladder scored 0–10 across 8 candidates; "I design complex systems and ship code." won 8.9/10 over "I design systems and ship code." (8.4) — "complex" earned its slot by signaling tradeoff-management on systems work without aspirational language. **Twitter card** root upgraded `summary` → `summary_large_image` (ends 75×75 thumb regression). **Implementation** via Next 16 metadata-file colocation: `src/app/opengraph-image.tsx` default-exports `ImageResponse` (Satori under the hood), with required exports `alt`, `size`, `contentType` per Next convention. Single PNG (~22KB) propagates to every route — layout's `openGraph` picks it up automatically; `[slug]/page.tsx` `generateMetadata` references `/opengraph-image` explicitly because Next's metadata merging *replaces* (not deep-merges) the `openGraph` object when a child returns one, so the parent layout's auto-resolved colocated image would otherwise be dropped on project pages. Geist Bold loaded via `src/lib/og-fonts.ts` reading `node_modules/geist/dist/fonts/geist-sans/Geist-Bold.ttf` and slicing `buffer.buffer` to ArrayBuffer. **Token-audit exemption**: Satori runs a flat CSS subset (no Tailwind, no CSS variables) so the OG file inlines `#FAFAFA`/`#18181B` hex literals — tagged with `// audit-ignore` comments mirroring `globals.css:35-44` source-of-truth. **Next 16.2 ESM gotcha caught and fixed**: `next.config.ts` is now compiled to ESM where `__dirname` is undefined (`ReferenceError`); replaced `path.join(__dirname)` with `path.dirname(fileURLToPath(import.meta.url))`. The user's *global* CLAUDE.md prescribes the old `__dirname` pattern; flagged for global update (unrelated to this PR). **X chrome wordmark clipping** caught during preview: `lg:grid-cols-2` made each cell ~486px wide but X chrome was fixed at 600px — `overflow-hidden` on the ScaledFrame was clipping the trailing `k` of "Branislav Benčík". Fixed by switching playground to single-column layout with `style={{ width: N }}` fixed-width chrome cards (no responsive grid). **Stale-local-main worktree trap caught at /ship**: `git diff main..HEAD` showed 75 files / 8240 insertions because local `main` was at PR #16 while `og-images` was branched off the latest remote main (PR #32); `git diff origin/main...HEAD` showed the actual focused diff. Worth remembering: in multi-worktree setups, only one worktree can checkout `main`, so the local ref grows stale fast — `origin/main` is the trustworthy reference for branch scope. **Files**: created `src/app/opengraph-image.tsx`, `src/lib/og-fonts.ts`, `src/app/playground/og-preview/{page,PreviewMatrix,ScaledFrame,chrome,types}.tsx` + `variants/V1Wordmark.tsx`; modified `src/app/layout.tsx` (em-dash title + new desc + summary_large_image + removed manual avatar.jpg references — auto-resolves via colocation), `src/app/[slug]/page.tsx` (dropped name suffix from project titles + explicit `/opengraph-image` reference), `next.config.ts` (ESM-safe `fileURLToPath`). **Docs sync**: README/CLAUDE.md/DESIGN.md/GUIDELINES.md/.impeccable.md — no updates needed (no token, component, behavior, voice, or page-table changes; metadata copy isn't tracked by any doc; `/playground/og-preview` follows existing precedent of unlisted playground routes). **Memory written**: `feedback_external_render_surface_chrome.md` (for OG / favicons / app icons, playground must mock consumer's chrome — Slack, LinkedIn, X, iMessage — not just render at real dimensions on bare canvas). Build clean, 14 static pages including `/opengraph-image` as ○ Static prerendered.

49. Hero chip refactor + cursor-following hover-preview card with landing animation (2026-05-01) — Multi-conversation arc on `fix-hero-statement-links` worktree branch that replaced the superscript-citation pattern with inline word-chips + 320×180 cursor-following hover-preview card + animated entrance. **Visual treatment**: `CitationLink` rewritten from `<sup>` superscript to `chip-link chip-color-blue link-underline` chip system. Editorial blue (`--citation-link` `#0946D7`) at rest, no rest underline; hover triggers `.link-underline::after` traveling-line animation (320ms `cubic-bezier(0.65, 0, 0.35, 1)` scaleX with origin flip — same primitive as nav/footer for vocabulary cohesion). Lede prose stays primary (Zinc 900); revertted from a brief secondary-chrome experiment after user found "actual secondary text looks poor" — restored Nav + Footer + lede to all-primary. Iterated through arrow-in-chip → arrow-in-card → no-arrow → mobile-only-arrow before landing on `<span class="chip-arrow">↗</span>` rendered via `@media (hover: none) { .chip-arrow { display: inline-block } }` (touch devices get the glyph next to chip text; hover-capable devices hide it). Lede sentence swap: dropped Skoala chip + "rabbit hole" sentence; added chipless judgment sentence "I focus on the path of maximum leverage in heavy tradeoffs" (matches *Warmly Precise* register; no em-dash). 4 chips shipped: Service design / n8n / Schneider / Sideproject. **Hover-preview card** (320×180, cursor-following, 280px outer with case-study chrome at `.card-style-casestudy`): mounted via `<HoverPreview />` inside the hero `<section>`. Component scans DOM for `[data-hover-preview-src]` on mousemove; cursor-tracked via imperative `el.style.transform` on rAF; edge-aware quadrant flip near viewport edges. Hardware-gated via `useSyncExternalStore((hover: hover) and (pointer: fine))` so touch devices return `null`. Real PNG artifacts at 840×474 (3× retina) replaced placeholder SVGs: `service-design.png`, `n8n.png`, `schneider.png`, `reprio.png`. Captions: "Service design of the learning system" / "n8n workflow for speech evaluation" / "Configurator for Electrical Panels" / "Chat with AI to prioritize tasks". **Entrance animation iteration arc** (the longest detour this session): tried `@starting-style` first per Emil-design-eng framework — didn't fire visibly because the wrapper's imperative cursor transform updates at frame N+1 while the conditionally-mounted card animates at frame N (off-screen at stale (0,0)). Swapped to `@keyframes` on element creation — same race. Final mechanism: **always-mounted card with `data-visible` attribute toggle**, animated via CSS `transition` on the attribute selector. Card pre-staged in DOM at `opacity: 0; transform: translateY(20px) scale(0.97)`; user hovers → React flips `data-visible="true"` → `[data-visible="true"]` selector applies `opacity: 1; transform: translateY(0) scale(1)` → CSS transition smoothly animates between them. Race eliminated because element exists in DOM from hydration onward; only attribute changes when user hovers. Final values: 320ms `cubic-bezier(0.16, 1, 0.3, 1)` translateY+scale+opacity, `transform-origin: center top` (card grows downward as it lands). Tuning ladder: 200ms/8px → 240ms/10px → 280ms/16px → 320ms/20px+scale (each "littlebit more" iteration after user couldn't perceive). 800ms/30px diagnostic confirmed the mechanism worked; dialed back to taste. Reduced-motion fallback: `transition: opacity 120ms linear; transform: none`. **Mobile fix**: removed SLOT-mode entirely (was a fallback rendering preview into a `.preview-slot-mobile` div below the lede). Designer dev-tools mobile-sim with mouse pointer kept `(pointer: fine)` true at 375px viewport so SLOT never activated; production touch devices worked but two-tap pattern was friction-heavy for visitors who'd scroll past. Replaced with platform-native one-tap-navigates + inline ↗ glyph as expectation-setter. Hybrid (desktop preview + mobile direct-nav) wins over forced symmetry. **Content tweaks**: TeaTime tag "+12 B2B Clients" → "+12 clients". **Tooling**: `.gitignore` adds `.claude/scheduled_tasks.lock` + `.claude/scheduled_tasks.json` (machine-local CronCreate artifacts). Cron `1cfa5e4a` scheduled for 2026-05-04 to trigger playground cleanup PR (session-only — likely won't survive). **Docs sync at /ship**: `docs/DESIGN.md` § Citation Link rewritten (drop superscript framing, document chip system + tokens + utilities); new § Hover Preview Card section (chrome + sizing + mounting + entrance animation + hardware gating). `GUIDELINES.md` § Motion gains the cursor-overlay rule (always-mounted + attribute-toggle, never `@keyframes` on conditional render); § Content rule for citation links rewritten to current state (editorial blue, traveling underline on hover, mobile inline ↗). README verified clean (no stack/route/dep changes). Memories written: `feedback_attribute_toggle_for_cursor_overlays` (cursor-following overlays use always-mounted + attribute toggle). Memory updated: `project_hero_citations_visual_exploration` rewritten to reflect actual final live state (was stale — described combo C with secondary text, but live shipped primary text + blue chips). Build passes 12 static pages
48. Hero citation copy refactor + lede width/mobile size (2026-04-27) — Two-channel copy + token pass on `fix-hero-links` worktree branch. **Citation chip rule established** (saved as `project_hero_chip_naming_rule` memory): the visible superscript label names whatever the inline prose hasn't already named — chip earns its space by adding new information. Earlier candidate rules failed: pure tool names forced a Figma×3 collision and a Sideproject-as-non-tool exception; pure artifact-types like "FigJam map" / "Skoala flow" doubled words across chips and named the *format* not the *artifact*. The "what's-new-given-prose" rule yields a 4-of-5 minimal-change pass: only Teatime chip moves (`Teatime → Service design`); n8n / Schneider / Sideproject / Skoala chips stay because the inline word ("automations" / "prototypes" / "code" / "states") already supplies the artifact context. **Cursor labels rewritten in mixed register** (not uniform active voice — earlier draft tried that and was wrong): imperative-active when the artifact does something for the user (#2 `Evaluates English speaking test recordings with AI`, #4 `Chat with AI to prioritize your tasks`); declarative noun-phrase when the artifact IS a thing/system (#1 `End-to-end service design for a language school`, #5 `Slides as parametrized CMS components`); past-participle declarative when the interesting fact is how it was made (#3 `Built in a 1-day Design Sprint`). **Skoala framing rejection-and-refinement chain** (saved as `feedback_system_thinking_over_quantity` memory): walked from `100 versions of a presentation slide` (quantity, original — sounded obsessive) → `Component set: 20 slides with 2–10 variants each` (over-selling) → `200+ slide variations` (just bigger number) → `Slides as parametrized CMS components` (system-thinking; matches what's actually in the Figma file: 16 slide types × multiple variants × mobile/tablet × content-type variants). User explicitly preferred abstraction-vocabulary over count-vocabulary even when offered higher numbers. **Teatime chip term arc**: rejected candidate-narrowing (Service blueprint / Accountability model / System architecture each named one frame; would have required narrowing href to that frame) in favor of `Service design` (covers all ~10 distinct artifacts in the FigJam: PRD, Course cycle, time-investment calc, Business strategy, Deliverables map, Accountability model, User journeys, Testing system architecture, Course Cycle Touchpoints, AI process overview); kept href broad to parent canvas so chip-cursor pair stays accurate as the FigJam evolves. **Lede tokens**: `--max-width-lede` 842→872px (cleaner desktop wrap; user's wrap test rejected my 864px grid suggestion since it pushed a key word to the next line; 872÷8=109 lands on grid AND ~73ch reading width — dead center of 65–75ch comfort window). `--font-size-lede` at `max-md:` 18→17px to restore hierarchy from `--font-size-body` 18px (which inherits from desktop at mobile, leaving lede ≡ body in size, weight, color, family — only line-height 1.45 vs 1.6 differentiates). User's first instinct 15px would have collapsed lede onto `--font-size-label` token (15) and risked inverted hierarchy if lede ever sits adjacent to body on mobile; pushed back to 17 (single-step reduction, preserves rung between body 18 and label 15) — user accepted "unique value" tradeoff. **Worktree-port footgun caught and resolved**: user reported "edits don't show after refresh"; lsof + `cwd` lookup revealed `:3007` was serving `hero-responsivity` worktree, not `fix-hero-links`. Started a `next dev` on `:3000` for the active worktree; verified new strings via curl. Diagnostic command worth remembering: `lsof -nP -iTCP:<port> -sTCP:LISTEN` plus `lsof -p <pid> | awk '$4=="cwd"'` resolves the wrong-codebase symptom in two commands — exactly the failure mode `feedback_surface_localhost.md` warns about. **Docs sync at /ship**: README verified clean (no stack/route/dep changes). `docs/DESIGN.md` does not cite `--max-width-lede` value or `--font-size-lede` mobile override anywhere (no stale numbers to fix). `.impeccable.md`, `GUIDELINES.md`, `CLAUDE.md` untouched (no voice/behavior/convention shift). Memories written: `project_hero_chip_naming_rule` (chip = what prose hasn't named) and `feedback_system_thinking_over_quantity` (abstraction-vocabulary > count-vocabulary in microcopy about past work). Build passes 11 static pages

47. Hero byline weight + tracking polish (2026-04-27) — Two-channel tweak on `.type-byline` in `src/app/globals.css`: `font-weight: 300 → 400` and `letter-spacing: -0.01em → 0`. **Why both at once:** they share one principle — at 56px / VC Honey Banner cut, light-300 + negative tracking belong to an editorial register (Vogue masthead, Aesop product copy) that conflicts with the Warmly Precise voice anchored on Craig Mod / Matuschak as voice references (both write in book weight, not light). Book-weight 400 + neutral tracking lets "Hi, I'm Branislav," read as a spoken greeting, not a designed flourish, while preserving 3-step weight contrast against the Display 700. Mobile resilience: at the 40px Banner-cut floor, weight 300 was rendering thinner than designed on standard-DPI displays; 400 holds. Added a comment block to `.type-byline` capturing the WHY (mirrors the `.type-lede` precedent immediately below). Convergent independent judgment: user had already arrived at the same two changes in his own playground exploration before asking — saved as `feedback_type_instinct_reliable` memory (his perceptual read on display type is calibrated; validate the instinct rather than relitigate). **Docs sync:** `docs/DESIGN.md` got a new Byline row in the Typography Scale table (VC Honey serif / 56 desktop / 40 mobile / 400 / 0 / 1.15) and a one-sentence addition to Global Rules acknowledging VC Honey as the third type family. **Out-of-scope drift surfaced but NOT fixed in this PR:** Display / H1 / H2 rows in DESIGN.md still cite pre-`design-tokens-v2` values (Display 48/600/-0.05em vs reality 56/700/-0.036em); flagged in What's Next for its own focused follow-up. README verified clean (no stack, route, or dep changes). Build passes 10 static pages

46. README — AI collaboration scaffolding section (2026-04-27) — Reworked the "Built with Claude Code" section in `README.md` from two terse paragraphs into named subsections so the setup is legible to outside readers. Added a **The status line is the dashboard** subsection covering the custom `~/.claude/statusline.sh` (model + branch · 15-block context % bar with green/yellow/red thresholds · live dev-server detection via `lsof` filtering for `next-server` / `vite` / `astro dev` / `nuxt dev` / `bun dev` whose cwd is inside the repo, printed as `:port branch-it's-running-for`) and pairs it with the convention of running the dev server for any visual work — explains why this matters when multiple worktrees run in parallel (terminal/tmux statusline misleads; lsof can't lie). **The MDs are the system:** new ownership table for `CLAUDE.md` / `.impeccable.md` / `docs/DESIGN.md` / `GUIDELINES.md` / `docs/STATUS.md` / `.claude/commands/ship.md` with the conflict-resolution rule called out (each .md has one job; if two disagree, escalate). **Skills I lean on:** named `impeccable` (`craft` / `teach` / `extract`), `impeccable critique` (framed as adversarial guardrail against AI-slop defaults — generic gradients, hover-scale on text, monotone grids), `emil-design-eng`, the wider design family (`polish`, `typeset`, `arrange`, `distill`, `bolder`, `quieter`, `colorize`, `delight`, `harden`), `superpowers:brainstorming` + `:writing-plans`, the Vercel plugin's auto-loaded session context + `vercel:react-best-practices`, and `/ship`. Playground-driven variant decisions promoted into the skills list (it's a named procedure, behaves like a skill). **Practices worth stealing:** quantified comparison tables with status-quo column, one-deliverable-per-session worktree-isolated, STATUS.md as living journal. **Cuts during review:** dropped a draft "How the AI gets better over time" memory-loop section per user feedback (memory is an Anthropic platform feature, not the user's contribution; the contribution is surfacing memory rules into checked-in `GUIDELINES.md` like the `feedback_no_hover_scale_on_text` cite — different thing). Dropped the `audit-tokens.sh` bullet from practices section per user feedback. README drift check: stack/scripts/repo-map all verified accurate against `package.json` and worktree state. Docs sync: no governance-doc updates needed (only README.md changed)

45. Typography + spacing + color discipline pass (2026-04-27) — Single-channel hierarchy sweep on `typography-color-finetune` branched off `origin/main` (post session 44 design-tokens-v2 merge). **Color discipline:** discarded `--text-tertiary` tier (3 → 2 text colors); promoted all 7 usages to secondary across `ProjectMetaRow`, `NextProjectSection`, `playground/page.tsx` (×4), `not-found.tsx`. Promoted case-study body prose primary across `mdx-components.tsx` (`<p>`, `<ol>`, `<li>`), `ProjectHeader` intro, `WorkSection` description; `ContributionList` headline `type-subheading` → `type-heading` (matches every other H2). `ImpactBar` labels secondary → primary (size/weight delta carries hierarchy). Captions stay secondary (figure-meta is intentional exception, not prose). **Spacing ladder 32·64·96·128·192:** landing subhead→first-item 96→64, item→item 64→96, section break 128→192 desktop / 120→160 tablet / 96→128 mobile (1:2 confident-double per editorial pattern in Stripe Press / NYT longreads). Case study detail: WorkSection header→first-image 56→96 (off-grid 56 corrected to ladder), image→image 64→96; ContributionList → ImpactBar tightened to 64 via new `tight` prop on ImpactBar (text→text rule per `feedback_text_gap_vs_image_gap` memory: text tiers are tightly readable, images need 96). Selected projects flat gallery: hero→first-image 192→96, gap-12→gap-24. **Hero + citations:** citation superscript 10px → 12px (legible on external monitors at desk distance), hard-fixed `font-size: 12px` instead of `0.5em` so it doesn't shrink with parent lede at mobile breakpoints; added `cursorLabel` prop to `CitationLink` and content-description labels to all 5 hero citations (PRD: "Personalized English learning that proves HR ROI"; n8n: "Evaluates English speaking test recordings with AI"; Schneider: "Made in 1 day during Design Sprint"; Sideproject: "Reprio — todo app where an LLM prioritizes"; Skoala: "100 versions of a presentation slide" — content over action since these are inline citations not card affordances). Hero top padding pt-20 → pt-32 (nav-to-hero ratio 1:2.9 → 1:1.8; rejected uniform pt-section since it pushes hero below fold on 1366×768). `type-lede` line-height 1.3 → 1.45 (clears the 9px citation-superscript collision; computed as exact-clear at 18px lede × 1.45 = 26.1px line gap = 9px between baselines = 9px superscript clearance). **Captions + cover:** figcaptions `type-caption` → `type-label` (13 → 15px) since 13px was a 28% drop from 18px body; keep secondary color (meta-content register intentional). `ProjectHeader` gains `coverCaption` prop with `<figure>` + `<figcaption>` rendering; `[slug]/page.tsx` extracts orphaned captions from filtered cover-matching first-image entries (Crowdberry: "Investment opportunities at a glance..."; NNspect: "Dataset overview..."; Sakurabook: "A rental space listing...") — schema-free wiring, no Keystatic config change needed. **Footer hover bug:** stripped `motion-safe:hover:scale-[1.01]` from email button — sibling links (GitHub/LinkedIn/Resume) used only `link-underline` and felt visually stable; the email button's 1.01 scale read as font-weight change because subpixel rendering on text glyphs at non-1.0 scale thickens stroke edges. Aligned "Copied" tooltip with CursorFollower pill (`type-allcaps` → `type-caption`, added `rounded-full`, padding `px-2 py-1` → `px-3 py-1.5`). **Playground card:** description gets `leading-[1.45]!` (Tailwind 4 cascade quirk: user-authored `@layer utilities` outranks Tailwind arbitrary values, so the override needs `!important` to escape `type-body`'s baked 1.6). **Image swap:** `teatime-ws1-i3.png` replaced with corrected annotated email screenshot. **Docs sync at /ship:** `docs/DESIGN.md` Color Tokens (removed tertiary line), Spacing & Rhythm table (192/160/128 + 96/96/72; documented text-vs-image rule + new ladder), Citation Link snippet (0.5em → 12px). `GUIDELINES.md` Motion: added "no transform on inline text" rule paired with existing transform-only rule. README verified clean. Memories written: `feedback_craig_mod_voice_not_visual` (Craig Mod is voice/content reference only, not visual-design template), `feedback_text_gap_vs_image_gap` (text→text 64, image→image 96, section break 192), `feedback_body_prose_is_primary` (body prose on case study pages always primary regardless of render path), `feedback_no_hover_scale_on_text` (never apply hover:scale-* to inline text — subpixel rendering reads as weight change)

44. Editorial masthead hero + page-title tier + schema merge (2026-04-26) — Multi-conversation arc on `design-tokens-v2` branch that shipped the editorial-masthead identity, new typography tier, and a content schema cleanup. **Hero (B-Light spec):** Banner Light 300 byline ("Hi, I'm Branislav,") + Geist Bold 700 display ("Generalist Product Designer.") + Geist Regular 400 lede; head-up-body-down anatomy preserved. Decided via `/playground` scaffolding (4 hero size variants × multiple weight/letter-spacing tests + citation comparison). Decision flow: rejected E2 serif lede on content-type grounds (lede is enumeration of evidence — PRD · n8n · Schneider · Sideproject · Skoala — not prose; lists scan in sans, not chant in serif); reverted lede weight 300→400 via UX-over-UI rule (Regular more readable + consistent with body copy elsewhere). **VC Honey serif** loaded via `next/font/local` in two optical cuts: Banner (display 40+) and Deck (body 14-32). **Page-title tier:** new `--font-size-page-title: 42px` token + `.type-page-title` utility (weight 700, line-height 1.1, geometrically uniform 1.333× ratio between Display 56 and Heading 32); `ProjectHeader.tsx` swapped from `type-display` so case study detail H1s read as chapter openers, not brand declarations — honors the dual-register principle (`project_detail_vs_landing` memory). Size picked from playground 4-cell comparison (40/42/48/56) with full-fidelity Skoala metadata + intro + hero image; user pushed back on initial 40 pick with geometric uniformity argument (1.327× geometric mean → 42–43). **Title field merge:** collapsed `title`+`tagline` schema fields into single `title` field across `keystatic.config.tsx`; renamed `tagline` prop to `title` across `CaseStudyCard.tsx`, `NextProjectCard.tsx`, `NextProjectSection.tsx`; updated `src/app/page.tsx` ProjectEntry type + card constructors. New unified titles per project: Skoala "Designing CMS for financial literacy platform"; TeaTime "Redesigning a B2B Language School" + intro shortened (drops the "20+ companies for 15 years" first sentence per user "no need to brag" feedback); Crowdberry "Crowdfunding investment platform"; NNspect "ML training platform for material quality inspection"; Sakurabook "Shopify plugin for per-hour booking in Japan"; Reprio unchanged. Pattern note: case studies use verb-led ("Designing X for Y"), selected projects use descriptive ("X for Y"), playground items have their own format. **Font-variable cascade fix:** moved `geist/font/{sans,mono}` and VC Honey `.variable` classNames from `<body>` to `<html>` — Tailwind 4's `@theme inline { --font-mono: var(--font-geist-mono) }` declaration computes at `:root`; with `--font-geist-mono` only on body it computed to invalid and every `var(--font-mono)` reference silently rendered as sans (caught via 20px sanity-check row in playground showing `var(--font-mono)` ≠ hardcoded `"GeistMono"`). Now fixed — `.type-allcaps`, `.type-status`, `.citation-sup`, `.type-tag-chip` all render in actual mono. **Citation `↗` arrow** forced to mono via `font-family: var(--font-mono)` on `.citation-sup` so the glyph is drawn by Geist Mono not the system fallback. **`/playground` private design lab** kept (7 hero variants for reference: A, B, B-Light, B-Light-2, B-Light-3, B-Light-Spaced, E, E2, H) but pruned of the citation comparison scaffolding + case-study-title-size scaffolding once decisions were made. Memories written: `feedback_content_type_drives_serif_vs_sans` (read paragraph aloud — list → sans, idea → serif, overrides serif voice tier), `feedback_no_aspirational_typography` (type matches actual writing pattern, not aspirational future register), `feedback_ux_over_ui_when_conflict` (visual-symmetry/mirrored-weights arguments lose to legibility/consistency arguments), `feedback_serif_optical_cuts` (Banner for 40+, Deck for 14-32; pick cut by size before weight). README verified — VC Honey is implementation detail, /playground intentionally not listed. `docs/DESIGN.md` typography table is now stale (still shows old Display 48 / H1 32 SemiBold) and should be synced in a follow-up per `feedback_docs_no_manual_writing`

43. Audit P2 polish — tablet, nav, footer hover (2026-04-26) — Three small responsive fixes on `audit-p2-polish` branched off `origin/main` (post session 42 ship-prep merge). New intermediate `@media (max-width: 1023px)` block in `globals.css` tightens `--spacing-content-x` 32→24, `--spacing-detail` 112→96, `--spacing-section` 144→120 so iPad-portrait viewports (768–1023px) no longer render as compressed desktop; type stays at desktop scale via cascading override (default desktop → max-lg intermediate → max-md mobile). Nav (`Nav.tsx:58`) gains `gap-x-3 whitespace-nowrap` on the flex row — guards against label wrap on narrow landscape mobile (iPhone SE 812×375). Footer email button (`Footer.tsx:143`) gets `origin-left motion-safe:transition-transform motion-safe:duration-[180ms] motion-safe:ease-out motion-safe:hover:scale-[1.01] active:scale-[0.99]` — matches the landing card transform-only hover pattern; `origin-left` keeps the existing `link-underline ::after` reveal anchored to its visual edge instead of drifting inward. Build clean. README verified — no drift. Session also produced the full `design-tokens-v2` plan (8-token / 3-weight system) that ships next as a separate PR off post-merge `main`. Plan reasoning includes: bottom-up audit feedback memory (reverse-engineer validated usage, don't impose top-down ratio), single-channel differentiation rule (size OR weight OR kicker, never stack), avoid-confusable-token-names rule (`subheadline` vs `subheading` was a system flaw not a misreading)

42. Ship-prep — audit, GUIDELINES.md, /ship docs sync (2026-04-26) — Comprehensive pre-ship sweep on `ship-prep` branch. Audit score 18/20 (Excellent) via three parallel Explore agents (a11y, perf+responsive, anti-patterns). Real P1 fixes: em dashes in `reprio.yaml` and `crowdberry.yaml` → comma/period (DESIGN.md Rule 7); Lightbox touch targets `max-md:p-3` (48×48 mobile, was at 44×44 minimum); Lightbox caption gets `max-h-screen overflow-y-auto py-4` for 200% browser zoom resilience; Footer email button focus-visible ring (WCAG 2.4.7). Three audit findings reclassified as false positives during code review (CaseStudyCard arrow span has reserved layout box, citation `top` is static not animated, status-dot `animation: none` correctly resets to opacity 1). **Reprio decoupled from detail-page system**: `NextProjectCard` filters `type === "playground"` from rotation, `generateStaticParams` excludes playground, `ProjectPage` early `notFound()` for playground type. `/reprio` route gone; landing card still links to live product at `https://reprio.vercel.app/` via `PlaygroundCard`. README link updated. **Four-file documentation architecture established**: NEW `GUIDELINES.md` at root (~30 atomic rules across Interactions / Motion / A11y / Performance / Content; warmly-precise voice; audit P0/P1 findings seeded as enforceable rules). `CLAUDE.md` slimmed 1300→750 words with new Documentation Map at top; stack drift fixed (Next 15+MDX → Next 16+Keystatic+[slug] template). `docs/DESIGN.md` Section 5 spacing rewritten to actual values (144/96 desktop/mobile, was stale 80/120); new Citation Link subsection documenting the hero superscript pattern. `.claude/commands/ship.md` extended with **Step 1.7 Docs sync** (the four-file architecture enforcement: branch-change classification → propose specific diffs → user approves each → commit alongside code → "Docs touched" tag in PR description) and Step 7 PR template gets `## Docs touched` section. `.impeccable.md` already existed (run earlier; my planning `find` missed it because I searched for `PRODUCT.md` per public docs, not the actual skill-written `.impeccable.md` filename). `next.config.ts` gets `turbopack.root: path.join(__dirname)` per global preference (silenced multi-lockfile warning). `audit-tokens.sh` passes clean throughout. Build passes 9 static pages (was 10; Reprio removed). Memory write: `feedback_docs_no_manual_writing.md` captures workflow preference (Claude drafts, user approves at /ship choke point — never separate "remember to update docs" tasks)

41. Bug batch — audit, cursor, hero, rhythm (2026-04-26) — `postcss` override in `package.json` silences the moderate audit chain (`postcss<8.5.10 → next → @keystar/ui → @keystatic/* → geist`) without bumping `next` or `@keystatic/*`; vuln is a CSS-stringify XSS that has zero runtime surface in a static-author-CSS portfolio. CursorFollower stale-on-scroll bug fixed via `document.elementFromPoint` re-resolution gated on `hasMoved` ref + rAF-throttled `scroll` listener — pill now updates correctly when user scrolls without moving the mouse. Tooltip copy reduced to verbs (`Read case study` / `View project`, no trailing arrow on internals; `See it live ↗` on Reprio); cursor pill inverts to white-on-dark via `data-cursor-label-tone="inverted"` attribute on PlaygroundCard so the dark canvas gets a contrasting pill. Hero: `Blueprint` → `PRD` label pointing at Notion PRD URL; Schneider Figma URL updated to new starting node `28343:3731`. Typography: `type-card-title` 16/semi/reg → 15/bold/med (editorial register; size in token, weight via component classes — `font-bold` on `<strong>{company}</strong>` and `font-medium` on tagline `<span>`). Vertical rhythm: established a clean 1.5× modular scale on landing → cards 64 / header→first 96 (`mb-8` → `mb-24` × 3 sections) / section→section 144 (`--spacing-section` 112 → 144 desktop, 72 → 96 mobile); replaces the irregular 1:1.25:1.75 ratio that read as "cramped section headers." Year on landing: explored across three placements (inline `· year`, eyebrow above title, corner stamp `'YY` outside frame) and dropped — each placement surfaced a real visual conflict (sentence-mode appendage, 4th vertical zone, meta-pair illusion with section header in same `type-allcaps` register). Year stays in Keystatic schema and YAMLs; detail pages render year unchanged; landing cards return to the original 3-zone composition (image + single meta-row). Nav: top-right `GitHub`/`Resume` wrapper `text-text-secondary` → `text-text-primary` to match left-side wordmark tone. PlaygroundCard: outer frame radius `rounded-[12px]` → `rounded-[6px]` to match case study cards (image `rounded-t-[6px]` was already 6px, now flush with outer corner). Skoala year `2024–25` → `2025`, Teatime `2024–25` → `2025` in YAML. Statusline (`~/.claude/statusline.sh`, outside repo) gained dev-server-process whitelist filter (`next-server`, `vite`, `astro dev`, etc.) so MCP servers and other non-HTTP listeners that bind ports under the repo root no longer surface as fake dev-server indicators. Memory updates: saved `feedback_status_quo_in_comparison_tables.md` (rule: every scored comparison table must include a "status quo" column), updated `feedback_cursor_as_action_label.md` to reflect new copy direction and year-on-card decision

40. TeaTime role + resume refresh (2026-04-24) — unified both TeaTime references in `src/content/projects/teatime.yaml` to `Co-owner`: the header `role` field and the landing-card tag both now read "Co-owner" (replaces the previous mixed state where the tag said "Co-founder" and role said "Co-owner"). Swapped in a fresh `public/resume.pdf` from `~/Desktop/resume.pdf` (854 KB, up from 851 KB — content update, same static route). No component or token changes

39. Caption column token cleanup (2026-04-24) — captions and the 404 body swap from `max-w-text` (552px) to `max-w-column` (640px); the stale `--max-width-text` token is removed from `globals.css`. CLAUDE.md layout-token line updated so the documented design system matches what ships

38. Playground section + Reprio + cursor-follow action labels (2026-04-24) — added a third landing row "Playground" between Case studies and Selected projects, carrying built-and-shipped side projects; Reprio ships as the first playground item with its own thumbnail (`public/images/reprio/reprio-thumb.png`) and YAML content (`src/content/projects/reprio.yaml`, `type: playground`). New `PlaygroundCard` component for the row and new `CursorFollower` component that renders a cursor-tracking action label on hover ("View case study" / "View project" / "Open Reprio ↗") — layered on top of the existing section labels, not replacing them. Keystatic schema extended to accept the playground type. Minor polish on `CaseStudyCard`, `CaptionedImage`, `CitationLink`, `HeroStatement`. README updated to list Reprio under a new Playground line.

37. Editorial polish — landing + detail (2026-04-24) — split landing into "Case studies" / "Selected projects" sections with `type-allcaps` headers and flex-aligned 1px horizontal rule (label flush-left, rule extends to frame edge via `flex-1`, vertically centered with `items-center` + `leading-none` + a -1px rule nudge to land on cap-midline); deleted earlier per-card "Case study" inverted chip experiment in favor of group-level taxonomy. Detail-page rhythm bumped: `--spacing-section/--spacing-detail` 80→112px desktop, 48→72px mobile; `WorkSection` header→images gap `mb-10`→`mb-14`; figure→figure `gap-12`→`gap-16` to compensate for caption weight. ImpactBar stacked rows now get `max-md:border-t` dividers. Hero citation links: parens not brackets, color `#0645AD`→`#0946D7` (brighter, 7.4:1 contrast on white), size `0.43em`→`0.5em` (~14px), baseline lift `-0.6em`→`-0.75em`. CaseStudyCard hover: subtle `motion-safe:hover:scale-[1.01]`, duration `120ms`→`180ms`. Responsive fixes: dropped `max-md:px-0` on landing wrapper; `truncate` on card title now neutralized via `max-md:whitespace-normal max-md:overflow-visible`; `NextProjectSection` inner frame gets `max-lg:px-content-x`. Image treatment tuning across all projects via YAML `background`/`paddingSides` flags; binary swaps for `skoala-ws1-i1`, `skoala-ws1-i2`, `teatime-ws1-i3`, `teatime-ws1-i4`. Captions added/refined for NNspect, Sakurabook, Crowdberry hero thumbs. Net code delta favored deletion: `.type-tag-chip-inverted` utility removed, `isCaseStudy` prop removed from CaseStudyCard


1. Scaffold — static export, MDX, Figma tokens, route stubs (`bd15291`)
2. Landing page — Nav, case study cards, selected project grid (`990a8aa`)
3. Thumbnails — added project images, pushed to git to fix Vercel display (`583a3f2`, `6a9c53c`)
4. Skoala case study — MetadataRow, CaseStudyHeader, ContributionList, ImpactBar, WorkSection, CaptionedImage components + full /skoala MDX page; CaseStudyCard extracted to shared component
5. Skoala polish — WorkSection refactored to Tailwind, description prop, 552px text column fixed (added --max-width-* to globals.css), 100px image spacing, centered captions, double border at page end (two 1px lines, 8px gap)
6. CaseStudyCard fixes — correct image aspect ratio (744×432), TagPill shared component, space-between layout pins buttons to bottom of left column
7. Design token cleanup — added type-subheadline, type-body-*, type-allcaps, type-button utilities; baked font-weight into type-h*; gap-* utilities; accent-live color token; removed all hardcoded px values and inline gap styles from all 10 components; deleted docs/figma-tokens.ts
8. TeaTime case study — full /teatime MDX page: CaseStudyHeader, ContributionList, ImpactBar, 3 WorkSections (ACQUIRE/DIFFERENTIATE/AUTOMATE), 8 CaptionedImages with custom width/border/bg/rounded props, NextProject linking to /skoala
9. Selected project overlays — Radix Dialog overlay system; ProjectOverlay + SelectedProjectContent components; NNspect overlay with 3 images wired up; stub pages deleted; Sakurabook card visible, overlay TODO; overlay refined: 1224px wide centered panel, full height, no radius, 48px padding, fade+scale animation, #000/40% backdrop
10. Dedicated project pages — converted overlay system to /nnspect and /sakurabook dedicated pages; removed ProjectOverlay.tsx and @radix-ui/react-dialog; landing page cards link via href prop; Sakurabook content added (workspace images pending)
11. Selected project header — 552px centered text block (metadata, headline, description, tag) matching case study hero column width
12. Token audit tooling + inline style fixes — audit-tokens.sh script; fixed all 6 inline style violations (gap-case-study, max-w-text, pl-[em], basis-[35%], CaptionedImage dynamic width); e2e responsive spec improved with overflowMustPass flag and image-load await
13. Responsive styling — registered max-xl/max-lg/max-md custom variants; responsive CSS variable overrides for typography + spacing; fixed spacing utilities to use var() (mb-section, py-section, px-content-x etc.); CaseStudyCard stacked layout with condensed bar at max-xl; selected projects 1-col at max-md; ImpactBar flex-wrap; Footer stacks; all 25 e2e responsive tests pass
14. Spacing tokens auto-scaling — moved spacing tokens from @theme inline to plain @theme; Tailwind now generates var() references for ALL derived utilities (mt-section, my-section, gap-section, pb-section, etc.); gap tokens moved to :root; removed 5 redundant custom @layer utilities spacing classes
15. Responsive consolidation — collapsed two-breakpoint system into single max-lg; CaseStudyCard drops intermediate tablet (flex-row) layout, mobile column layout now applies at all ≤1023px; all max-md: renamed to max-lg: across Nav, Footer, ImpactBar, CaptionedImage, page.tsx
16. CLAUDE.md rules — consolidated responsive critical rules into numbered list; clarified auto-scaling behaviour for typography, layout spacing, and nav
17. Image lightbox + next-project linking — Lightbox component (motion/react, portal, scroll lock, Escape key); CaptionedImage auto-wired; /nnspect ↔ /sakurabook cross-link with double-border separator + SelectedProjectCard teaser (`b7f7edd`)
18. Gallery lightbox — LightboxContext provider in layout.tsx; all CaptionedImage + CaseStudyHeader hero self-register; prev/next arrows, ← → keyboard nav, counter, captions; dark backdrop blur, cursor-zoom-out on backdrop, cursor-default over image (`1315510`)
19. Crowdberry selected project — /crowdberry page (Fintech, 2020) with 3 captioned images; added to landing page grid; each selected project page now shows two cards side-by-side (prev + next) forming circular chain nnspect → sakurabook → crowdberry → nnspect (`c4ce0ec`)
20. HeroStatement component — interactive 3-line headline with infinite auto-cycling tooltips (3.5s), hover interruption + resume from last hovered row, AnimatePresence fade (150ms); desktop two-column grid with fluid clamp on type-h1; tablet collapses to full-width headlines + shared tooltip below; type-h1 uses separate clamp formulas for desktop vs tablet (`b8662f5`)
22. Token audit cleanup (2026-04-10) — replaced inline `display:none` on grain SVG with `className="hidden"`; annotated the two legitimate dynamic inline styles (console.log CSS string, variable-font RAF weight) with `audit-ignore`; audit now passes clean (`ab0ea25`)
21. Critique polish pass (2026-04-10) — removed project aura glows (achromatic canvas); extracted shared ProjectMetaRow (Case Study pill · Year · Role · Domain) used on landing card + detail page header; inverted Case Study pill (bg-text-primary) on both surfaces; added `domain` field to Keystatic schema, backfilled 5 projects; added hero status line with pulsing green live indicator (`--accent-live`); fixed siteUrl + replaced JPEG favicon with SVG monogram placeholder; dropped HeroStatement skeleton; simplified card hover (dropped ambient shadow, bumped inset hairline); cleaned up dead tokens; deleted orphaned ProjectTags component
23. Extract cleanup + workflow polish (2026-04-10) — deleted two orphaned components (`MetadataRow`, `TagPill`) with zero callers anywhere in the codebase; added a mandatory session-start branching rule to `CLAUDE.md` (never commit directly to `main`); extended the `ship` skill to handle new-branch first-push (`git push -u origin HEAD`) and added a PR-creation step via `gh pr create`, with an explicit no-merge instruction so the user approves
28. Hero mobile fix (2026-04-13) — shrunk mobile `--font-size-display` from 36px to 32px (matches h1) so "Product designer fluent" no longer overflows narrow screens; added mobile override for `.type-status` (13px, line-height 1.45, tighter tracking); dropped "BASED" from the typewriter status line and injected a `\n` after "REMOTE FRIENDLY |" so mobile wraps `OPEN TO NEW ROLES` to a second line via `max-md:whitespace-pre-line` while desktop keeps the single-line layout via `whitespace-nowrap`

30. Worktree workflow (2026-04-15) — added `/ship` step 0.5 that detects git worktrees via `git rev-parse --git-dir` and rewrote step 8 to skip `git checkout main` when running inside a worktree (would otherwise fail because the root checkout already has `main`). Paired with a new `cc <branch>` shell function in `~/.zshrc` that creates or resumes a worktree under `.claude/worktrees/<branch>` branched off latest `origin/main`, then `cd`s in and launches Claude. Unlocks parallel Claude sessions without branch-switching conflicts. Dev-server `SessionStart` hook and automated worktree pruning deferred until the manual workflow reveals the real friction points

29. Typeset polish (2026-04-14) — replaced `StatusTypewriter` typewriter mechanic with a scramble-and-decode animation (30ms tick, lock-pointer left-to-right, reduced-motion bypass, CLS-safe via invisible-placeholder width reservation, cursor removed); upgraded `.animate-hero-content-in` from a 200ms opacity fade to a 560ms slide-up + opacity (`translateY(14px) → 0`, `cubic-bezier(0.16, 1, 0.3, 1)`) with reduced-motion fallback; new hero copy "20+ APPS DESIGNED | AI-NATIVE"; mobile `--font-size-h1` → 24px (restores hierarchy vs 32px display on mobile); ImpactBar tuned to 32px @ 700 / -0.04em (was 40px @ 600 / -0.05em) so stat numbers become peer with `type-h1` work-section headlines instead of dominating them; renamed `.type-hero` utility → `.type-stat` (single callsite in `ImpactBar`), deleted dead orphan `.type-stat` utility + unused `--font-size-stat: 64px` and `--line-height-stat` tokens; CaseStudyCard description `type-body-s` → `type-body-m`; `ContributionList` items `type-body-m` → `type-body-l` (matches `WorkSection`); `NextProjectCard` now threads `description` from Keystatic through `NextProjectSection` → `CaseStudyCard`; Footer adds `GitHub` link first (then LinkedIn, Resume); TeaTime card description rewritten ("Built acquisition pipeline (+12 B2B clients) and AI-evaluated speaking test automated with n8n."); hero subhead letter-spacing 0.05em → 0.07em; README stack line updated to list all three Geist variants

30. Hero scramble in Geist Mono + frame narrowed to 1080 (2026-04-14) — reconciled two divergent `StatusTypewriter` takes across overlapping working-tree states: kept main's scramble/matrix decode effect and new copy (`20+ APPS DESIGNED | AI-NATIVE`) but swapped the pixel font for Geist Mono at 13px/400 and softened the color from `text-text-primary` to `text-text-secondary` for a quieter register; narrowed `--max-width-frame` 1288px → 1080px and retuned `CaseStudyCard` `<Image>` dimensions 1288×748 → 1080×628 so the first card fits a 14" MacBook fold with the next card's metadata row peeking above; dropped the now-unused `--font-pixel` CSS variable and the mobile-only `.type-status` `@media` override that no longer applies (`1258403`)

31. Nav + footer density pass (2026-04-14) — new `.type-nav` utility class (13px / 400 / line-height 1) shared by nav links and footer links, quieter register than `.type-link`; nav + footer heights dropped `h-14` → `h-13` (56px → 52px); `PIN_THRESHOLD` bumped 56 → 52 to maintain the "pin-zone = one nav height" invariant from the earlier top-of-page artifact fix; `Nav.tsx` scroll logic rewritten from velocity-filtered (`delta > DELTA`) to position-based (`delta > 0` hides, `delta < 0` shows) — 1px precision, no jitter grace, predictable behavior regardless of scroll speed; `--spacing-detail` tightened 100px → 64px; `CaseStudyCard` inner gap `gap-10` → `gap-6` and `pb-[48px]` → `pb-detail` token; README stack line drops "Pixel Square" (no longer imported after PR #12 swapped the hero status from pixel to mono)

34. Crowdberry image/caption fix (2026-04-23) — swapped `crowdberry-ws1.png` ↔ `crowdberry-ws2.png` `src` values in `src/content/projects/crowdberry.yaml` so each workflow image now matches its caption (split-view visual under the "Split view pairs a guided walkthrough..." caption; dashboard visual under the "Portfolio Dashboard..." caption). Dashboard now lands last as requested. Alt text and captions untouched — only the image paths were transposed

36. Hero wrap + sizing hotfix (2026-04-23) — dropped the forced `<br className="max-md:hidden" />` in `HeroStatement.tsx` (source of two bugs: desktop orphan line after "rabbit hole"; mobile "holeto" collision from hidden `<br>` leaving no whitespace between sibling text nodes); replaced with `&nbsp;` between "hole" and "to" so the pair never splits across a line. Added `text-wrap: pretty` to `.type-hero-prose` so modern browsers balance the tail clause and avoid widows without any width cap. Lowered desktop `--font-size-hero-prose` 28 → 24 (mobile already 24) — reads calmer at the 960px frame, measure lands in the 70–80ch readable band. Shipped off a fresh `hotfix/hero-wrap` branch off `origin/main` since `landing-card-refresh` is still unmerged

35. Messaging overhaul + Levitate card hover + two-mode frame (2026-04-23) — replaced specialist headline ("Product designer fluent in B2B systems & code" + StatusTypewriter) with generalist prose paragraph anchored by 5 superscript citations (Blueprint PDF, n8n, Schneider Figma proto, Sideproject→Reprio, Skoala Figma); new `CitationLink` component with `.citation-sup`/`.citation`/`.citation-arrow` utilities (blue `#2F4BB8`→`#25399A` hover, nudge-up-right arrow). Narrowed `--max-width-frame` 1080 → 960; hero prose `--font-size-hero-prose` 32 → 28; hero unbounded (flush with nav/footer) with forced `<br className="max-md:hidden" />` before "to the mapping of states" so it lands on line 4 on desktop; terminal period closes the sentence. Cards now frame-bound (moved inside `max-w-frame` wrapper), separated by `gap-16` instead of `<hr>` dividers. New "Levitate" hover on `CaseStudyCard`: outer bordered frame never moves, inner content has `-m-px` + own border so they overlap pixel-exact at rest (single visible border); on hover, row bg fades to `surface-1` and inner scales `0.97` at 320ms `cubic-bezier(0.23, 1, 0.32, 1)`, revealing a surface-1 ring framed by two borders; `will-change-transform` + `backface-visibility:hidden` keeps images sharp during the scale. Two-mode frame system: `src/app/layout.tsx` drops `border-x` (landing now frameless); new `src/app/[slug]/layout.tsx` wraps case study routes in full 4-sided `border` so detail pages read as framed long-form documents, landing reads as gallery of tiles. Nav: GitHub link added alongside Resume (`w/ ExternalArrow` extracted to `src/components/icons/`); `border-b` moved from full-width `<header>` to inner `<nav>` (at `max-w-frame` width) and fades from `border-transparent` → `border-surface-2` only when `isDetail && scrolled` (tracked independently of `prefers-reduced-motion` since it's a state indicator, not motion); `border-b` removed from landing nav entirely. Footer: dropped `border-t`, `pt-section`, and `·` separators between GitHub/LinkedIn/Resume; footer now flush with content using card's own `pb-detail` for breathing; GitHub link added (first). `CaseStudyCard` gains `borderless` prop; `NextProjectSection` passes it so the case study layout's frame serves as the card's frame with no double-border collision. New typography utilities: `type-section-h2` (applied to `WorkSection` headlines), `type-hero-prose`, `type-kicker` (latter was removed from hero after user chose to drop the "Previously @Applifting · Co-founded TeaTime" kicker — résumé surfaces carry that info). Retired: `StatusTypewriter.tsx`, `FullWidthDivider.tsx`. Shipped `public/blueprint.pdf` (Blueprint citation target). Built `playground/hero-width.html` — self-contained HTML with 7 strip controls (frame width, hero size, canvas tone, card hover variant, hero inset, hero borders, card width) for variant exploration; URL-query-backed state. Citation labels decided as: `Blueprint`, `n8n`, `Schneider`, `Sideproject` (capitalized to match proper-noun style), `Skoala`. Kicker discarded

27. Edge-to-edge cards + full-width dividers (2026-04-12) — reworked `CaseStudyCard` to full-bleed the thumbnail inside the card (no horizontal padding); whole-card hover offset (-1.5px/-1.5px) reveals a `surface-1` background with a border on the revealed edges; image gets a `border-b border-surface-2` to hold its bottom edge; card border itself is transparent at rest, `border-surface-2` on hover/focus. Added reusable `FullWidthDivider` component that breaks out of the `max-w-frame` container via `w-[100vw] -ml-[calc((100vw-100%)/2)]` to render true viewport-width 1px separators, matching the nav border style. Landing page renders a divider above each card; `NextProjectSection` wraps its label and card with dividers; `Footer` gains `border-t border-surface-2` (outside the frame, so naturally full-width). Footer layout restructured to mirror the nav's left/right split (email left, LinkedIn · Resume right) and the "Curiouser" variable-weight easter egg is hidden via `hidden` class (kept in code for future use). Swapped remaining thumbnail JPGs for PNGs.

26. Card hover lift + content pass (2026-04-11) — replaced the original inset-ring hover on `CaseStudyCard` with an E2B-style image lift: image wrapper translates up-left 6px over 150ms on hover/focus, revealing a 1px `border-surface-3` slot frame and matching ring overlay underneath; scoped the effect to a `group/image` so hovering metadata/headline no longer moves the image; moved the image ring to an overlay div so `box-shadow: inset` renders above the image content (previous placement painted it below the opaque image); verified the fix with a one-off Playwright hover screenshot spec; added E2B to `.impeccable.md` references and added an em-dash ban to the anti-references list; fixed README drift (`static export into ./out` → `production Next build`; `DESIGN.md` → `docs/DESIGN.md`); assorted edits across `Nav`, `HeroStatement`, `ProjectMetaRow`, `TabAttention`, layout, `[slug]` page, and project YAML

25. README refresh + ship drift check (2026-04-11) — rewrote `README.md` to address four personas (recruiters, designers, engineers, AI-curious) and fixed stale claims (Keystatic now powers content, `output: 'export'` removed, case studies rendered from `[slug]/page.tsx` not MDX folders); added Step 1.5 "README drift check" to `.claude/commands/ship.md` so every ship run re-reads the README and fixes factual drift before committing; pruned three merged local branches (`extract-cleanup`, `critique-polish`, `redesign`)

24. Interaction pass (2026-04-10) — scroll-aware auto-hide nav (64px threshold, delta-based, motion-safe); contextual left slot (wordmark on `/`, `← Back` on detail pages linking to `/`); unified `type-link` utility (14px, line-height 1) so underlines sit tight under letters; nav + footer grid structure aligned; external `↗` arrow on Resume + LinkedIn (optical align tuned); lightbox parity with detail pages (background flag threaded through LightboxContext → Lightbox, 24px matte padding, fully opaque backdrop); case study card border restored via overlay div with ring; Case Study pill text softened to `/80` opacity; hero stagger simplified to single 200ms fade; footer Copied toast becomes inverted tooltip above button (no layout shift); footer `max-[1320]` → `max-[1320px]` bug fix (`0635ca2`)

## Known Issues
(none)

## Decisions
- Tailwind v4: tokens in `globals.css @theme`, not `tailwind.config.ts`
- Next.js 15 App Router, static export
- MDX pages live in route folders (`/skoala/page.mdx`), not `src/content/`
- `next/image` with `fill` + `aspect-ratio` works fine for static export — images just need to be committed
